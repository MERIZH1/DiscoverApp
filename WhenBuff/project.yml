name: WhenBuff
options:
  bundleIdPrefix: com.whenbuff
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

schemes:
  WhenBuff:
    build:
      targets:
        WhenBuff: all
    run:
      config: Release
    archive:
      config: Release

targets:
  WhenBuff:
    type: application
    platform: iOS
    sources:
      - path: Sources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.whenbuff.app
        INFOPLIST_FILE: Sources/Info.plist
        TARGETED_DEVICE_FAMILY: "1,2"
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        GENERATE_INFOPLIST_FILE: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: WhenBuffIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        SWIFT_VERSION: "5.0"
        SWIFT_STRICT_CONCURRENCY: minimal
